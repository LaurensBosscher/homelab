apiVersion: v1
kind: Service
metadata:
  name: nullclaw
  labels:
    app.kubernetes.io/name: nullclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nullclaw
  ports:
    - name: http
      port: 3000
      targetPort: http
