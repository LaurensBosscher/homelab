apiVersion: v1
kind: Service
metadata:
  name: beszel
  labels:
    app: beszel
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      protocol: TCP
  selector:
    app: beszel
