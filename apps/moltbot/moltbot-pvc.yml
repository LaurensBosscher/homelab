apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: moltbot-pvc
  labels:
    app.kubernetes.io/name: moltbot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-2replicas-europe
  resources:
    requests:
      storage: 5Gi
