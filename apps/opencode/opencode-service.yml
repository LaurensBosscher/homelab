apiVersion: v1
kind: Service
metadata:
  name: opencode
  labels:
    app.kubernetes.io/name: opencode
spec:
  type: ClusterIP
  ports:
    - port: 4096
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: opencode
