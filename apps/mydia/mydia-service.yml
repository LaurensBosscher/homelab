apiVersion: v1
kind: Service
metadata:
  name: mydia
  labels:
    app.kubernetes.io/name: mydia
spec:
  selector:
    app: mydia
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      name: http
