apiVersion: v1
kind: Service
metadata:
  name: technitium
  labels:
    app: technitium
spec:
  type: NodePort
  selector:
    app: technitium
  ports:
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
