apiVersion: v1
kind: Service
metadata:
  name: shelfmark
  namespace: books
  labels:
    app: shelfmark
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
  selector:
    app: shelfmark
