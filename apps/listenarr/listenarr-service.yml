apiVersion: v1
kind: Service
metadata:
  name: listenarr
  labels:
    app: listenarr
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4545
      targetPort: 4545
      protocol: TCP
  selector:
    app: listenarr
