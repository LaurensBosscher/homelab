apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nanobot-data
  namespace: nanobot
  labels:
    app.kubernetes.io/name: nanobot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-2replicas-europe
  resources:
    requests:
      storage: 2Gi
